import React from 'react';

const DriftSimulator: React.FC = () => (
  <div style={{ padding: 16, background: '#222', color: '#fff' }}>
    Drift Simulator (stub)
  </div>
);

export default DriftSimulator;
